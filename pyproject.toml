[project]
name = "local-dispatch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "plotly>=5.24.1",
    "pytest>=8.3.5",
]
